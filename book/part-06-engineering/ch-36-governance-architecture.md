---
chapter: 36
part: VI
title: Ch 36｜治理架構 ⸺ 在高彈性系統中控制管理複雜度
slug: governance-architecture
agent: SA
skills_used:
  - sa/arch-microservice
  - sa/observability
  - shared/domain-fintech
domain_case: CASE-FIN-009
reviewers: [PM, QA, Orchestrator]
status: draft
word_count_target: 7000
---

# Ch 36｜治理架構
## ⸺ 在高彈性系統中控制管理複雜度

> **前置閱讀**：[Ch 34 架構適應度函式](./ch-34-fitness-functions.md)、[Ch 33 ADR 與架構知識管理](./ch-33-adr-architecture-knowledge.md)
> **下游章節**：[Ch 37 AI-Native 架構](../part-07-ai-era/ch-38-ai-native-architecture.md)（AI 系統同樣面臨治理問題，但複雜度再乘一倍）

---

## 36.1 冷觀察 ⸺ 新 CTO 的第一週，與一個跨境支付平台的稽核噩夢

### 場景一：新 CTO 的第一週

2026 年初，虛構跨境金融平台 **NovaPay**（`CASE-FIN-009`）迎來新任 CTO Vera。

她上任第一件事，只問了工程團隊兩個問題。

**問題一：「給我一份系統現在的配置地圖。」**

工程師花了三天，拼出一份文件。但沒有人能保證它和 prod 環境一致——因為最後一次更新是八個月前。

**問題二：「如果我今天把這個 flag 關掉，會發生什麼？」**

沉默。

不是工程師不聰明。是沒有人有足夠的信心回答。

### 場景二：十四個月後的稽核失敗

NovaPay 在 18 個月內為了應付 12 個國家的法規差異，陸續開了 70 幾個 feature flag：KYC 驗證等級、手續費計算規則、詐欺偵測門檻、資料留存期限……每一個都是當時的合理決策，每一個都沒有人記錄「為什麼這樣設」。

某天，金融監理機關來電：「請提交 2025 年 Q3 對台灣用戶適用的風控判斷規則，以及該規則的生效時間與核准紀錄。」

法務給了工程團隊 72 小時。

工程師找出了現在的 flag 狀態，但找不到三個月前的狀態。因為沒有配置版本紀錄——系統只保存「現在的值」，沒有保存「這個值是什麼時候、被誰、為什麼改成這樣的」。

最終，NovaPay 以「資料不完整」被要求限期改善，並支付六位數罰款。

---

這不是孤例。我見過電商平台在黑色星期五當天找不到「昨晚哪個 config 被改了」；見過 SaaS 平台的新工程師花三週才搞清楚「什麼是這個系統的正常行為」；見過醫療系統因為沒有人敢動任何設定，功能請求平均等待 14 週。

這些系統都有一個共同點：**它們被設計得非常有彈性，但彈性沒有被治理。**

> 📌 [圖示建議 36-1]
> *畫一條時間軸：Day 0（系統上線，彈性設計讓大家很開心）→ Month 6（feature flag 數量開始爬升）→ Month 12（原始設計師離職）→ Month 18（沒有人知道系統現在的完整狀態）→ Month 24（稽核失敗 / 事故發生）。每個節點標注「治理負債累積量」，曲線呈指數成長。*

---

## 36.2 真問題 ⸺ 彈性是乘法器，它把治理缺失放大

「高彈性系統」的問題不是彈性本身，而是**彈性在沒有治理機制的情況下會以指數方式製造複雜度**。

### 36.2.1 彈性的狀態爆炸

一個系統有 N 個獨立的布林 feature flag，就有 2^N 種理論上可能的配置狀態。

- N = 10 → 1,024 種
- N = 30 → 超過十億種
- N = 70（NovaPay 的數量）→ 理論上是個天文數字

但在實務上，「彈性」不只有布林 flag：

- **多值配置**：一個 config 有 5 個選項，就乘以 5 倍狀態空間
- **依賴關係**：flag A 只在 flag B 開著時有意義；flag C 和 flag D 不能同時開
- **環境差異**：dev / staging / prod 的 config 不一樣
- **時序依賴**：某個 config 在某個時間點是對的，在另一個時間點是錯的

每一個「彈性點」都是一個治理負債項目。設計時沒有配套的治理機制，這些負債會以指數方式複利成長。

### 36.2.2 治理缺失的三種死法

```
死法一：配置漂移（Configuration Drift）
    各環境的 config 各自演化，沒有人追蹤差異
    → 「在我的機器上可以跑」的分散式版本
    → 最終：prod 行為和任何人的預期都不一樣

死法二：知識流失（Knowledge Decay）
    原始設計意圖只存在原始設計師的腦袋裡
    → 人一走，沒有人知道為什麼這樣設定
    → 最終：沒有人敢改，系統成為禁忌神器

死法三：隱性耦合（Invisible Coupling）
    config 之間有依賴關係但沒有文件記錄
    → 改一個看似無關的 flag，觸發遠端的奇怪行為
    → 最終：每次改動都是一次未爆彈拆除
```

主大綱第 21–22 章談了分散式系統的狀態一致性，第 34 章談了架構適應度函式。**但系統分析很少觸碰治理（Governance）這個議題。治理不是管理學的詞彙，它是一個工程問題：你怎麼確保「允許改變的部分」被改變時，系統行為仍然可預測、可追蹤、可復原？**

> 📌 [圖示建議 36-2]
> *繪製「治理缺失的三種死法」樹狀圖。根節點：高彈性系統。三個分支：配置漂移 / 知識流失 / 隱性耦合。每個分支下標示典型症狀（工程師說什麼話）和最終結局（事故 / 稽核失敗 / 禁忌神器）。*

---

## 36.3 決策框架 ⸺ 治理系統的五個層次

這五個層次不是獨立的選項，而是一個完整的堆疊。從下到上依序建立。

### 36.3.1 層次一：機制與政策分離（Mechanism-Policy Separation）

最根本的設計原則，也是其他四個層次的基礎。

把系統切成兩層：

| 層次 | 特性 | 設計目標 | 例子 |
|---|---|---|---|
| **機制層** | 固定、通用、穩定 | 永遠不需要為了業務需求而改 | Event log 格式、Contract schema、API interface |
| **政策層** | 可配置、可版本化、可稽核 | 是唯一被允許改變的層 | 哪個角色能核准哪類請求、哪些 flag 在哪個環境開啟 |

**核心規則：複雜度只允許在政策層增長。機制層一旦設計完成，不因業務需求而改動。**

實作形式：政策層通常是版本控制下的 JSON / YAML / Rego（OPA）檔案，而不是程式碼邏輯。每一次政策變動就是一個 git commit，有時間戳、有作者、有 diff、有 review 紀錄。

```
錯誤的方式：
    if (user.role === 'admin' && feature_flag_x && env === 'prod') { ... }
    // 業務規則混在程式碼裡，無法在不改 code 的情況下修改

正確的方式：
    policy.evaluate('can_access_feature_x', { user, env })
    // 機制：evaluate() 永遠不變
    // 政策：policy 定義存在版本控制的 policy.rego，可獨立修改、測試、回滾
```

NovaPay 的稽核問題根源就在這裡：KYC 規則和手續費邏輯混在程式碼 if-else 裡，flag 狀態存在資料庫但沒有版本歷程。修法：把所有合規規則抽成 OPA policy 檔，每次變更走 PR review + 自動 audit log。

### 36.3.2 層次二：黃金路徑（Golden Path / Paved Road）

**讓 80% 的使用場景走不需要行使彈性的預設路徑。**

黃金路徑不是限制，是「預鋪好的最優解」。偏離黃金路徑是被允許的，但需要付出治理成本：留下偏離理由、通過對應層級的審批、承擔後續維護責任。

```
黃金路徑示意：

標準流程 ─────────────────────────────────→ 產出
              ↑
     80% 的案例走這裡
     零配置，零治理成本

偏離點 ──→ 偏離路徑 A ──→ 需要 [記錄原因 + 審批]
         ↘ 偏離路徑 B ──→ 需要 [記錄原因 + ADR + 人工審查]
```

黃金路徑的設計原則：
- 覆蓋 80% 使用場景的預設值，不是「最彈性的預設值」
- 偏離的代價要明確標示（不是懲罰，是透明化）
- 黃金路徑本身需要定期重新評估（過時的黃金路徑比沒有更糟）

### 36.3.3 層次三：複雜度預算（Complexity Budget）

彈性不是無限資源，應該有明確的上限。

**複雜度預算的三個量化指標：**

| 指標 | 說明 | 建議上限 |
|---|---|---|
| Active flag count | 目前生效中的 feature flag 數量 | 依團隊規模設定，建議 < 2 × 工程師人數 |
| Configuration depth | Config 層次的巢狀深度 | ≤ 3 層 |
| Active API versions | 同時維護的 API 版本數 | ≤ 2 個主版本 |

超出預算時不是「警告」，而是**阻斷**：不允許新增 flag / config 選項，直到舊的被清理。

在 CI pipeline 中用 architecture fitness functions 自動量測（參見 [Ch 34](./ch-34-fitness-functions.md)）：

```yaml
# 範例：在 CI 中檢查 feature flag 數量
check_complexity_budget:
  script:
    - flag_count=$(grep -r "feature_flag" config/ | wc -l)
    - if [ $flag_count -gt $MAX_FLAGS ]; then
        echo "FAIL: Feature flag count $flag_count exceeds budget $MAX_FLAGS";
        exit 1;
      fi
```

這把「彈性擴張」從被動感知（事後才發現太複雜）變成主動阻斷（CI 直接擋住）。

### 36.3.4 層次四：可觀測性驅動治理（Observability-First Governance）

**無法觀測的彈性等於無法治理。**

治理需要的可觀測性不是一般的 logging 和 metrics，而是**決策稽核軌跡（Decision Audit Trail）**：

```
每一個「彈性被行使」的事件必須記錄：
    ├── Who:  誰做了這個決策？（人類 or 系統）
    ├── What: 改了什麼？（before / after diff）
    ├── Why:  依據哪個政策版本？有沒有 ticket / ADR？
    ├── When: 什麼時間生效？
    └── Where: 影響哪些環境 / 服務 / 用戶？
```

這不只是 audit log，它是治理的 source of truth。沒有這層，後續的複雜度預算、分層審批都無從建立。

實作模式：把 config 變更視同程式碼部署，走相同的 GitOps 流程：

```
config 變更 PR
    → 自動 diff 展示（影響面分析）
    → reviewer 審核
    → merge → 自動部署到 staging
    → smoke test 通過 → 部署到 prod
    → 變更記錄寫入 audit log（含 Who/What/Why/When/Where）
```

> 📌 [圖示建議 36-3]
> *畫一個「決策稽核軌跡」的時間線圖。X 軸是時間，Y 軸是系統配置狀態。每個配置變更點標一個菱形，附上 Who/What/Why。一旁對比一個「無稽核軌跡」的時間線，同樣的時間段只有一條平線，沒有任何可追蹤資訊。*

### 36.3.5 層次五：分層審批（Tiered Approval Gateway）

不是所有彈性都需要相同的治理成本。過度治理低風險彈性會讓工程師繞過治理系統；治理缺失高風險彈性是事故前兆。

**風險 × 審批成本矩陣：**

| 風險等級 | 彈性類型 | 治理機制 | 典型例子 |
|---|---|---|---|
| **低** | 無行為影響的配置調整 | 自動批准 + 日誌 | 改 log level、調整 timeout 值 |
| **中** | 改變系統行為路徑 | Peer review + diff 展示 | 開啟 / 關閉 feature flag、角色權限調整 |
| **高** | 影響合規 / 安全 / 架構 | ADR + 人工審核 + staging 驗證 | 資料留存政策、加密方式、外部整合端點 |
| **緊急** | 線上緊急修復 | 事後補文件（< 24h）+ 事後審查 | 生產事故中的緊急 config rollback |

**緊急通道是治理的壓力閥**。完全不允許緊急通道，工程師遇到事故會繞過所有治理；允許緊急通道但要求事後補文件，治理機制才能在壓力下存活。

> 📌 [圖示建議 36-4]
> *繪製「分層審批流程圖」。入口：一個配置變更請求。第一個菱形：風險等級判斷（低/中/高/緊急）。四條路徑各自走向不同的審批流程，最終匯入 audit log。標注每條路徑的典型等待時間（低：自動秒批；中：4h；高：24-48h；緊急：先行後補）。*

---

## 36.4 踩坑清單

- **治理工具比被治理的系統更複雜** — OPA、Cedar、自建 policy engine：如果沒有人維護得了它，它就會成為另一個禁忌神器。先從版本控制下的 YAML 政策檔開始，不要一開始就上最複雜的 policy language。

- **黃金路徑過時了比沒有更糟** — 黃金路徑覆蓋的場景如果已經不代表主流使用模式，工程師每次都要「偏離」，治理成本反而上升。黃金路徑需要每季重新評估。

- **複雜度預算設得太鬆等於沒設** — 如果上限是 500 個 flag 而現在只有 50 個，這個預算沒有任何約束力。預算應設在「讓人感到有點緊張」的位置，才能驅動清理行為。

- **稽核日誌沒有人看** — 光記錄不分析等於白費。稽核軌跡需要有人定期回顧（例如每週的 config review），否則它只是一堆沒人看的 log。

- **緊急通道被濫用** — 如果緊急通道走了 30% 的 config 變更，代表正常流程太慢。解法不是取消緊急通道，而是加速正常流程（自動化 review、縮短 staging 驗證時間）。

- **把「不彈性」當成解法** — 有時最好的治理決策是「把這個配置選項拿掉，直接 hardcode」。彈性有成本，如果沒有真實的業務需求驅動，不要保留彈性點。

---

## 36.5 交付清單

- **彈性點目錄（Flexibility Inventory）**：列出所有 feature flag、config 選項、可配置參數；標注每個的風險等級、最後修改時間、負責人。
- **機制 / 政策邊界圖**：一張圖標示哪些部分是機制層（不能改），哪些是政策層（可改但有 SLA）。
- **黃金路徑文件**：描述標準使用場景的預設配置，標注偏離條件與代價。
- **複雜度預算設定**：三個指標的上限值，以及 CI 中的自動量測 script。
- **決策稽核軌跡格式**：Who / What / Why / When / Where 的記錄 schema。
- **分層審批矩陣**：低 / 中 / 高 / 緊急四個等級的觸發條件與審批 SLA。
- **緊急通道 SOP**：緊急 config 變更的操作程序 + 事後補文件的 deadline 與格式。
